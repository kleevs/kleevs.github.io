export declare class Transaction {
    private _transaction;
    constructor(_transaction: any);
    execute<T>(sql: string): Promise<T>;
    execute<T>(sql: string, params: any): Promise<T>;
    select<T>(sql: string): Promise<T[]>;
    select<T>(sql: string, params: any): Promise<T[]>;
}
export declare class DataBase {
    private _db;
    constructor(name: string, version: string, description: string, size: string, callback: (db: DataBase) => void);
    transaction<T>(sql: string): Promise<any[]>;
    transaction<T>(callback: (tr: Transaction) => T): Promise<T>;
}
